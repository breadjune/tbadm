<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.trafficboss.tbadm.repository.MemberRepository">

    <select id="findByUserId" resultType="kr.co.trafficboss.tbadm.dao.Member">
        SELECT
            no
        ,   user_id    AS userId
        ,   user_pw    AS userPw
        ,   user_name  AS userName
        ,   user_level AS userLevel
        ,   user_ip    AS userIp
        ,   reg_date   AS regDate
        ,   mod_date   AS modDate
        FROM TB_USER
        WHERE user_id = #{userId}
    </select>

</mapper>